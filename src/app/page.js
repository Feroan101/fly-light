import HomeClient from './HomeClient';

export const metadata = {
  title: 'Fly Light — School of Badminton',
  description: 'Welcome to Fly Light, a premier badminton coaching academy in Tamil Nadu. Expert coaches, state-ranked players, training programs for all levels.',
};

export default function HomePage() {
  return <HomeClient />;
}
